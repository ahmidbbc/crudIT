{% extends 'base.html.twig' %}

{% block title %}Danse{% endblock %}

{% block body %}
    <h1>Danse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ danse.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ danse.nom }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ danse.type }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ danse.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('danse_index') }}" class="btn btn-primary">Retour à a liste</a>

    <a href="{{ path('danse_edit', {'id': danse.id}) }}" class="btn btn-info">Modifier</a>

    {{ include('danse/_delete_form.html.twig') }}
{% endblock %}
